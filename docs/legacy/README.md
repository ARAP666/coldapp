# Legacy

Historical artifacts kept for reference only. **Not the source of truth.**

- `calculadora-fria.html` — the original single-file HTML prototype that
  FRÍA started as. The current Expo app in `app/` is the source of truth.
